# Configure TypeScript in NodeJS

* <https://www.youtube.com/watch?v=SEnAS_ooHeA>

* Install typescript globally

```bash
npm install -g typescript
```

* Check typescript version

```bash
tsc --version
```

* Create tsconfig.json file

```bash
tsc --init
```